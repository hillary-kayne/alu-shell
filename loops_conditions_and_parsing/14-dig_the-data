#!/usr/bin/env bash
# Display visitor IP and HTTP status code from Apache logs (handles malformed lines)
awk '
{
  ip = ($1 != "" ? $1 : "-")
  code = ($9 != "" ? $9 : "-")
  print ip, code
}
' apache-access.log
