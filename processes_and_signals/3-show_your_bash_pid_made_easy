#!/usr/bin/env bash
# Displays PID and process name of processes whose command line contains "bash"
for pid in /proc/[0-9]*; do
    pname=$(<"$pid/comm")
    if [[ $pname == *bash* ]]; then
        echo "${pid##*/} $pname"
    fi
done
